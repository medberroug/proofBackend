{
  "routes": [
    {
      "method": "GET",
      "path": "/draftorders",
      "handler": "draftorders.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/draftorders/count",
      "handler": "draftorders.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/draftorders/:id",
      "handler": "draftorders.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/draftorders",
      "handler": "draftorders.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/draftorders/:id",
      "handler": "draftorders.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/draftorders/:id",
      "handler": "draftorders.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
