{
  "routes": [
    {
      "method": "GET",
      "path": "/tax",
      "handler": "tax.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/tax",
      "handler": "tax.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/tax",
      "handler": "tax.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
